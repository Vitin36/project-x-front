import { Button } from "reactstrap";
import { styled } from "styled-components";

export const StyledInfoButtonPopover = styled(Button)`
    padding: 0.5em!important;
    font-size: 0.75em!important;
`